#include <stddef.h>

/*
 * This function takes a pointer to an array of integers
 * len must be the length of that array
 * end is the character to print after printing all the integers
 * provide '\0' for end if you want nothing printed after the
 * array.
 * Prints the array of integers to standard output with one
 * space inbetween integers.
 */
void printArray(int *arr, size_t len, char end);
